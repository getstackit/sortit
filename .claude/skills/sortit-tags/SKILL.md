---
name: sortit-tags
description: Inspect the Sortit tag catalog. Use when the user wants to see available tags before filtering or creating tagged issues.
allowed-tools: Bash(command sortit:*), Bash(sortit:*)
version: dev
---

# Sortit Tags

## Context

Current tag catalog: !`command sortit tags list`

## Command

```bash
command sortit tags list
```

Use this before issue search or creation when tag selection matters.
