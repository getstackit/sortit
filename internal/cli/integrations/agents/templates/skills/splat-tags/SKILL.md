---
name: splat-tags
description: Inspect the Splat tag catalog. Use when the user wants to see available tags before filtering or creating tagged issues.
version: {{VERSION}}
---

# Splat Tags

Use `command splat tags list` to fetch the tag catalog.

## Command

```bash
command splat tags list
```

Use this before issue search or creation when tag selection matters.
