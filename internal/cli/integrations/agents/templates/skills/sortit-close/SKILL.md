---
name: sortit-close
description: Close one or more Sortit issues. Use when the issue is resolved, obsolete, or intentionally closed out.
version: {{VERSION}}
---

# Sortit Close

Use `command sortit issues close <id> [id...]` to close issues.

## Command

```bash
command sortit issues close issue-000123 --closed-by "Jon"
```

Useful flags:
- `--closed-by <name>`
