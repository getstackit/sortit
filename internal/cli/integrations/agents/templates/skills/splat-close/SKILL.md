---
name: splat-close
description: Close one or more Splat issues. Use when the issue is resolved, obsolete, or intentionally closed out.
version: {{VERSION}}
---

# Splat Close

Use `command splat issues close <id> [id...]` to close issues.

## Command

```bash
command splat issues close issue-000123 --closed-by "Jon"
```

Useful flags:
- `--closed-by <name>`
