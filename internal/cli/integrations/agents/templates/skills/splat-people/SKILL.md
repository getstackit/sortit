---
name: splat-people
description: Inspect people analytics in Splat. Use when the user wants a person's issue profile or overlap between people.
version: {{VERSION}}
---

# Splat People

Available commands:

```bash
command splat people profile "Jon" --status all
command splat people correlations --status all
```

Use `profile` for one person and `correlations` for cross-person overlap.
