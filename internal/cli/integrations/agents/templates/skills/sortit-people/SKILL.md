---
name: sortit-people
description: Inspect people analytics in Sortit. Use when the user wants a person's issue profile or overlap between people.
version: {{VERSION}}
---

# Sortit People

Available commands:

```bash
command sortit people profile "Jon" --status all
command sortit people correlations --status all
```

Use `profile` for one person and `correlations` for cross-person overlap.
